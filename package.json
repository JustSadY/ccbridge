{
  "name": "ccbridge-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Cross-agent session bridge for local coding assistants",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "check": "npm run check --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
